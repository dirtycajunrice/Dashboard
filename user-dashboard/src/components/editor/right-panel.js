import React from 'react';

const RightPanel = ({styles}) => {
  return (
    <div className={styles.rightPanel}>
      {/* Add right panel content here */}
    </div>
  );
};

export default RightPanel;